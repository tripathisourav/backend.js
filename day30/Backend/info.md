day 118

Express Validator

type module in package.json is used so that we can write import statments instead of require


hm auth.controller se khud hi error bhejenge or phir us error ko kaise resolve kiya jayega usse hm error handling seekhanga

server still runs even after error comes because express has inbuilt error handler, gives error in html format 


for import statements we can use enviornmental variables only in those files where dotenv is imported
